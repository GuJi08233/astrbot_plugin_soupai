{
  "generate_llm_provider": {
    "description": "生成谜题的 LLM 服务商",
    "type": "string",
    "hint": "留空则使用系统默认的 LLM 服务商。建议使用智力大于等于DeepSeek0-V2 R1的模型。",
    "default": ""
  },
  "judge_llm_provider": {
    "description": "判断问答和生成提示的 LLM 服务商",
    "type": "string",
    "hint": "留空则使用系统默认的 LLM 服务商。不建议使用reasoning模型",
    "default": ""
  },
  "game_timeout": {
    "description": "游戏超时时间（秒）",
    "type": "int",
    "hint": "游戏超时时间，超过此时间自动结束游戏并揭晓答案",
    "default": 1800
  },
  "storage_max_size": {
    "description": "存储库最大容量",
    "type": "int",
    "hint": "存储库最多保存多少个备用故事",
    "default": 50
  },
  "auto_generate_start": {
    "description": "自动生成开始时间",
    "type": "int",
    "hint": "每天自动生成备用故事的开始时间（小时，24小时制）",
    "default": 3
  },
  "auto_generate_end": {
    "description": "自动生成结束时间",
    "type": "int",
    "hint": "每天自动生成备用故事的结束时间（小时，24小时制）",
    "default": 6
  },
  "puzzle_source_strategy": {
    "description": "谜题来源策略",
    "type": "string",
    "hint": "选择从哪个题库优先获取谜题",
    "default": "network_first",
    "options": [
      "network_first",
      "local_first",
      "custom_first",
      "random"
    ]
  },
  "reply_mode": {
    "description": "回复方式",
    "type": "string",
    "hint": "quote=引用提问者的原消息后再回答，多人同时提问时能分清在回答谁；direct=直接回复，不引用。部分平台不支持引用时会自动降级为直接回复。",
    "default": "quote",
    "options": [
      "quote",
      "direct"
    ]
  },
  "verification_limit": {
    "description": "每局验证次数上限",
    "type": "int",
    "hint": "每局游戏最多可使用多少次 /验证。该限制全程生效，不再只在提问次数用完后才计数。填 0 表示不限次数。",
    "default": 2
  },
  "judge_engine": {
    "description": "提问判定引擎",
    "type": "string",
    "hint": "决定「是/否/不重要/是也不是」由谁来判。llm=用上面配置的判断 LLM；jev=用 TypeSafe 的 Jev 模型，响应约 0.7 秒且只会返回这四个选项之一。仅影响提问判定——出题、提示、以及 /验证 提交答案一律由 LLM 处理。",
    "default": "llm",
    "options": [
      "llm",
      "jev"
    ]
  },
  "judge_fallback_to_llm": {
    "description": "Jev 判定不确定时改用 LLM",
    "type": "bool",
    "hint": "仅在判定引擎选 jev 时生效。开启后，置信度低于下面的门槛就改用 LLM 判定，牺牲一点速度换准确率。关闭则一律采信 Jev（置信度门槛不再生效），但请求失败时仍会提示稍后再试。",
    "default": true
  },
  "jev_api_key": {
    "description": "Jev API Key",
    "type": "string",
    "hint": "TypeSafe API Key。留空则即使开启上面的开关也不会启用。",
    "default": ""
  },
  "jev_base_url": {
    "description": "Jev API 地址",
    "type": "string",
    "hint": "官方为 https://api.typesafe.ai ，使用中转站则填中转地址（需支持原生 /v1/systemone 端点）。",
    "default": "https://api.typesafe.ai"
  },
  "jev_model": {
    "description": "Jev 模型名",
    "type": "string",
    "hint": "默认 jev-latest。可填 jev-preview 或锁定具体版本如 jev-1.13.0。",
    "default": "jev-latest"
  },
  "jev_judge_min_confidence": {
    "description": "Jev 判定的最低置信度",
    "type": "float",
    "hint": "低于此值就改用 LLM 判定。需要上面的兜底开关为开才生效。判错只影响一次提问，可以设宽松些；填 0 表示始终采信 Jev。",
    "default": 0.5
  }
}