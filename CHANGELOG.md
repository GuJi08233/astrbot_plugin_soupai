# 更新日志

版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## Unreleased

### 新增

- 网页管理页增加默认开启的每 5 秒自动刷新开关，刷新当前标签；页面隐藏、
  弹窗打开或已有请求时暂停，设置页不自动刷新。对局刷新保留问答与判定详情的
  展开状态及滚动位置。
- 使用过 Jev 的问答可展开判定详情，分别展示四个候选答案的概率、整体置信度、
  原判定选项、门槛及回退原因。最终回退到 LLM 时仍保留已有的 Jev 数据；
  请求未返回或旧问答未记录的数据不补造。
- 增加 `hint_llm_provider`，允许为 `/提示` 独立选择模型；留空跟随判断问答
  LLM。`judge_llm_provider` 继续负责问答判定和 `/验证` 答案验证。

### 修复

- 顶部「刷新」改为更新当前标签的数据，包含题库、会话、对局与设置。
- 设置页切换标签时保留未保存的修改；手动刷新前确认是否丢弃修改，请求期间
  新编辑的内容也不会被返回的数据覆盖。

## 1.3.0 - 2026-09-20

### 新增

- 网页管理页的「对局」标签会标出每一问是谁判的：`Jev 0.99`（Jev 直接判定，
  带置信度）、`LLM · Jev 回退`（Jev 失败或置信度不足后改用 LLM）、`LLM`
  （本来就配置走 LLM）。对局卡片上另有一行本局小结，例如
  `判定 Jev 4 · 回退 LLM 1`，用来快速看出 Jev 在这局的表现。
  判定来源只在网页端显示，群里的回复不受影响。

## 1.2.0 - 2026-09-20

### 修复

- **插件初始化钩子从未执行。** 钩子写成了 `init()`，而 AstrBot 只会调用
  `initialize()`——没有任何报错，它只是不运行。后果是网页管理接口的 14 条路由
  从未注册（打开页面报「未找到该路由」），后台自动出题也从未启动。
- **网页管理页无法与面板通信。** 发给面板的消息缺少 `kind: 'request'` 字段，
  面板会静默丢弃，页面所有请求都卡到超时。题库页因此一直是空的，看起来像是
  插件没有内置题库。
- **网页端所有二次确认操作失效。** 面板给插件页面的 iframe 没有 `allow-modals`
  权限，浏览器会忽略 `window.confirm()` 并直接返回 `false`，于是删除题目、
  重置记录、强制结束对局全部被静默取消。现在改用页面内的确认弹窗。

### 改进

- 网页端主题跟随 AstrBot 面板，不再只跟随操作系统（面板深色而系统浅色时，
  页面原本会整片发白）。
- 网络题库去重：289 条中有 101 条是重复题面，去重后为 **188 道**；同一题有多份
  抄本时保留内容最完整的那份。
- 网络题库每道题带上了由题面计算出的稳定 id，使用记录不再依赖题目在列表中的
  位置。**旧的使用记录与屏蔽记录按下标存储，去重后已无法对应**，插件启动时会
  把它们备份为 `*.byindex.json` 后丢弃，并在日志中说明。
- 网页端补充了加载骨架、Esc 关闭弹窗、Ctrl+Enter 保存；在只读题库上点「新增」
  会提示新题将存入自定义题库；AI 生成题目的等待上限放宽到 10 分钟。

## 1.1.0 - 2026-09-20

### 新增

- 网页管理页：题库浏览与编辑、会话出题记录、进行中对局、插件配置。
- 出题记录按会话（群 / 私聊）隔离，各自独立。
- 可选的 Jev 判定引擎，用于问答判定；`/验证` 始终不走 Jev。

### 修复

- 适配 AstrBot 4.28。

## v1.0.3 - 2025-09-06

- 网络题库改回随插件目录分发，clone 时一并获取。

## v1.0.2 - 2025-08-11

## v1.0.1 - 2025-08-09

- 隔离群会话，避免同一条提问被重复处理。

## v1.0.0 - 2025-08-02

- 首个版本。
