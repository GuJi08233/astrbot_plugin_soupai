# 更新日志

版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## Unreleased

### 新增

- `/验证` 改为百分制打分。模型分别为事实、动机、反转三项打分，按 35% /
  25% / 40% 合成总分；缺项按剩余维度归一化，不把缺项当 0 分。分项得分只进
  日志和网页管理页，群里只显示总分——公布分项等于告诉玩家还有哪块没想到。
- 达标线按难度递增：简单 60、普通 70、困难 80、666开挂了 90，取代原先
  「简单接受核心推理正确、其余必须完全还原」的四档等级判定。新增
  `verification_pass_score` 可用一个分数覆盖所有难度，填 0 表示跟随难度。
- 达标后本局不再自动结束：玩家可以 `/揭晓` 收场，也可以继续提问、把推理补
  完整后再验证冲高分，提问次数用完后同样可以。只有拿到 100 分才自动结束。
- 达标之后的验证不再消耗验证次数。揭晓、超时和次数用尽的收场消息都会带上
  本局最高得分。
- 网页管理页新增「历史」标签：对局结束后归档到插件数据目录的
  `game_history.json`，可按会话筛选、分页翻看，也可整体或按会话清理。
  列表不含汤底；点「查看复盘」才单独取一次，带出汤面、汤底、完整问答
  （含判定来源与 Jev 详情）、每一条提示，以及每次验证提交的内容、得分和
  三项分项。最多保留 200 局，超出后挤掉最旧的；没有任何问答、提示和验证
  的空局不归档。归档失败只记日志，不会拦住对局收尾。
- 验证记录进入对局状态：此前只有次数，复盘看不到玩家提交了什么、得了多少
  分。现在每次验证都记下原文、总分、三项分项、等级、达标线和是否计次。
- `/查看` 的每条问答标注判定来源，Jev 判定附带整体置信度（如 `Jev 0.87`），
  回退和直接使用 LLM 的分别标为 `LLM · Jev 回退`、`LLM · 追问判定`、
  `LLM · 复合提问`，末尾附本局各来源条数。措辞与网页对局页一致；此前的问答
  没有这项数据，不补造也不显示空标签。
- 网页管理页增加默认开启的每 5 秒自动刷新开关，刷新当前标签；页面隐藏、
  弹窗打开或已有请求时暂停，设置页不自动刷新。对局刷新保留问答与判定详情的
  展开状态及滚动位置。
- 使用过 Jev 的问答可展开判定详情，分别展示四个候选答案的概率、整体置信度、
  原判定选项、门槛及回退原因。最终回退到 LLM 时仍保留已有的 Jev 数据；
  请求未返回或旧问答未记录的数据不补造。
- 增加 `hint_llm_provider`，允许为 `/提示` 独立选择模型；留空跟随判断问答
  LLM。
- 增加 `verify_llm_provider`，允许为 `/验证` 独立选择完整推理验证模型；留空
  跟随 `judge_llm_provider`，后者也留空时使用当前会话模型。问答判定与 Jev
  回退仅使用 `judge_llm_provider`，可分别为问答选择快速模型、为验证选择
  推理能力较强的模型；生成和提示模型保持独立配置。
- 增加 `StoryCatalog` 旁路题库标注，保存题材、标签、故事摘要、因果链与关键
  反转。结果和向量缓存存放在插件数据目录的 `story_catalog.json`，网络题库
  原文件保持不变。题目内容变化后旧标注失效，列表只显示标注状态，正文须主动
  点击「查看标注」。
- 增加单题 LLM 标注，以及批量标注的范围预览、显式启动、进度、失败详情和
  取消功能。默认跳过有效标注，可勾选强制重标；已完成结果逐题保存，取消后仍
  保留，后续可重新预览并处理剩余题目。打开页面或预览不会启动批量模型任务。
- 新增及编辑统一通过 `save_story_checked()`，在网络、本地、自定义三个题库
  间执行精确检查与可选语义复核。同批生成和并发保存共用写入锁，生成发现重复
  时每道最多尝试 3 次（含首次）；生成或查重失败不入库。
- 增加 `annotation_llm_provider`，供标注与查重复核共用；留空依次跟随验证
  LLM、问答判定 LLM。`dedup_semantic_enabled` **默认开启**，每次入库可能
  额外调用模型；关闭后仍检查跨库相同汤面或汤底。
- 增加可选的 `dedup_embedding_provider` 与 `dedup_rerank_provider` 及网页
  服务商下拉。首次使用嵌入模型建立向量缓存；未配置时使用文字相似度召回，
  可能漏检改写题。重排分数只用于相关性排序，不是重复概率，最终由 LLM 复核。
- 增加 `generation_theme` 和 `generation_ideas` 默认配置；网页「AI 生成」
  可为本次选择预设或自定义题材、填写创作参考，留空沿用配置。

### 改进

- `/提示` 按玩家处境调整力度：最近 6 问里六成以上被判为「否」或「不重要」
  时判定为卡在走不通的线上，提示转为纠偏、把注意力移开那条线；方向正确时
  沿线推进；本局提问不超过 3 次仍算开局，只指出该问哪一类事情，不把注意力
  引向谜底所在的点。判定所需的进展统计由代码给出，不再让模型自己从问答里数。
- `/提示` 的维度必须取自固定清单，不得自造。
- 修复 `allow_list`：原实现按标点切分，整句进表（如「我在寝室洗了个头」），
  而且问句和回答被粘成「有凶手吗否」这种假词，导致「只用表内词」这条约束
  形同虚设。现改为 2-3 字滑窗、滤掉首尾是虚词的跨词片段、按出现频次排序，
  上限 80 个。
- Jev 与判断 LLM 共用事实判定规则，区分局部命中、真假混合、错误因果和未知无关细节。题面与汤底同时提供给两种引擎。
- 识别到的前文指代追问和复合提问直接使用判断 LLM；仅保留本局同一玩家最近三次有效问答，缺失指代依据或复核不可用时提示补充或拆分，不扣次数。网页标出直接使用 LLM 的原因。
- 判定调用设置总超时，校验异常置信度与矛盾概率分布；LLM 仅接受完整答案，避免前缀误截取及泄露解释。Jev 支持带 `/v1` 的地址，配置更新复用连接池并应用新凭证。
- 自动补充题库改为「开关 + 剩余题数阈值」：新增 `auto_generate_enabled` 可整体
  关闭自动生成，`auto_generate_min` 在本地存储库剩余未出题数低于该值时自动
  补满（默认 10，填 0 等于不自动补充）；取代原先按 `auto_generate_start` /
  `auto_generate_end` 固定时间段生成的做法，旧配置项不再生效。后台每 5 分钟
  检查一次，开关关闭时仅停止自动触发的生成，不影响 `/备用开始` 手动生成。

### 修复

- 验证判定本身没能完成（模型不可用或返回无法解析）时不再计分，也不消耗
  验证次数。
- 验证的用户提示词把玩家提交的内容包进 `<玩家推理>` 标记并声明为纯素材，
  降低玩家照抄输出格式（如提交「事实：100」）骗取高分的可能。这是缓解，
  不是完全防护。
- 会话内“强制结束”补齐管理员权限检查，不带指令前缀时也无法绕过。
- 题库采用临时文件写入后替换原文件；新增、编辑、删除或满库换题失败时明确报错并恢复内存数据，避免误报成功或丢失旧题。
- 网页结束对局、超时和插件卸载时清理会话监听器；题目准备完成即释放生成标记，结束后可立即重开。旧局清理和迟到的模型结果不会影响新局。
- 判定服务失败不再消耗提问次数或写入问答历史；无限提问模式也会正确统计有效提问数。会话内的“揭晓”统一兼容带前缀和不带前缀两种形式。
- 顶部「刷新」改为更新当前标签的数据，包含题库、会话、对局与设置。
- 设置页切换标签时保留未保存的修改；手动刷新前确认是否丢弃修改，请求期间
  新编辑的内容也不会被返回的数据覆盖。
- 新增或编辑题目被查重拒绝时保留编辑器和草稿，并显示错误；保存期间新编辑
  的内容也会保留。
- 区分入库失败和已保存后的标注缓存不完整：后者返回成功及提示，不再向用户
  报告题目未保存。

## 1.3.0 - 2026-09-20

### 新增

- 网页管理页的「对局」标签会标出每一问是谁判的：`Jev 0.99`（Jev 直接判定，
  带置信度）、`LLM · Jev 回退`（Jev 失败或置信度不足后改用 LLM）、`LLM`
  （本来就配置走 LLM）。对局卡片上另有一行本局小结，例如
  `判定 Jev 4 · 回退 LLM 1`，用来快速看出 Jev 在这局的表现。
  判定来源只在网页端显示，群里的回复不受影响。

## 1.2.0 - 2026-09-20

### 修复

- **插件初始化钩子从未执行。** 钩子写成了 `init()`，而 AstrBot 只会调用
  `initialize()`——没有任何报错，它只是不运行。后果是网页管理接口的 14 条路由
  从未注册（打开页面报「未找到该路由」），后台自动出题也从未启动。
- **网页管理页无法与面板通信。** 发给面板的消息缺少 `kind: 'request'` 字段，
  面板会静默丢弃，页面所有请求都卡到超时。题库页因此一直是空的，看起来像是
  插件没有内置题库。
- **网页端所有二次确认操作失效。** 面板给插件页面的 iframe 没有 `allow-modals`
  权限，浏览器会忽略 `window.confirm()` 并直接返回 `false`，于是删除题目、
  重置记录、强制结束对局全部被静默取消。现在改用页面内的确认弹窗。

### 改进

- 网页端主题跟随 AstrBot 面板，不再只跟随操作系统（面板深色而系统浅色时，
  页面原本会整片发白）。
- 网络题库去重：289 条中有 101 条是重复题面，去重后为 **188 道**；同一题有多份
  抄本时保留内容最完整的那份。
- 网络题库每道题带上了由题面计算出的稳定 id，使用记录不再依赖题目在列表中的
  位置。**旧的使用记录与屏蔽记录按下标存储，去重后已无法对应**，插件启动时会
  把它们备份为 `*.byindex.json` 后丢弃，并在日志中说明。
- 网页端补充了加载骨架、Esc 关闭弹窗、Ctrl+Enter 保存；在只读题库上点「新增」
  会提示新题将存入自定义题库；AI 生成题目的等待上限放宽到 10 分钟。

## 1.1.0 - 2026-09-20

### 新增

- 网页管理页：题库浏览与编辑、会话出题记录、进行中对局、插件配置。
- 出题记录按会话（群 / 私聊）隔离，各自独立。
- 可选的 Jev 判定引擎，用于问答判定；`/验证` 始终不走 Jev。

### 修复

- 适配 AstrBot 4.28。

## v1.0.3 - 2025-09-06

- 网络题库改回随插件目录分发，clone 时一并获取。

## v1.0.2 - 2025-08-11

## v1.0.1 - 2025-08-09

- 隔离群会话，避免同一条提问被重复处理。

## v1.0.0 - 2025-08-02

- 首个版本。
